import "./style.css";

const questions = [
  {
    text: "退屈な会話になると、つい別の角度からツッコミたくなる",
    type: "idea"
  },
  {
    text: "正しいかどうかより、まず面白いかどうかを見てしまう",
    type: "chaos"
  },
  {
    text: "議論で勝ちたいというより、相手の理屈を崩したくなる",
    type: "debate"
  },
  {
    text: "思いついた企画を最後までやる前に、次の企画を思いつく",
    type: "idea"
  },
  {
    text: "みんなが当然と思っているルールに『本当に？』と思う",
    type: "chaos"
  },
  {
    text: "相手を怒らせるつもりはないのに、なぜか煽ってると言われる",
    type: "debate"
  }
];

const debateTopics = [
  {
    title: "一生スマホ禁止 vs 一生ゲーム禁止",
    a: "スマホ禁止の方がマシ",
    b: "ゲーム禁止の方がマシ"
  },
  {
    title: "天才だけど飽き性 vs 凡人だけど継続力最強",
    a: "天才だけど飽き性",
    b: "凡人だけど継続力最強"
  },
  {
    title: "全員が本音を言う世界 vs 全員が空気を読む世界",
    a: "本音の世界",
    b: "空気を読む世界"
  }
];

const app = document.querySelector("#app");

app.innerHTML = `
  <header class="hero">
    <p class="badge">ENTP.JP</p>
    <h1>ENTPのための、謎に本気なサイト。</h1>
    <p>
      議論、発想、屁理屈、好奇心。<br>
      ここはENTPを研究するための遊び場です。
    </p>
    <div class="hero-buttons">
      <a href="#diagnosis">ENTP度診断</a>
      <a href="#debate">ディベートゲーム</a>
    </div>
  </header>

  <main>
    <section class="card">
      <h2>ENTPとは？</h2>
      <p>
        ENTPは、ただ議論が好きな人ではありません。
        退屈な前提を壊し、新しい可能性を見つけるタイプです。
      </p>
      <ul>
        <li>アイデアが出るのが早い</li>
        <li>常識を疑うのが好き</li>
        <li>議論で思考が加速する</li>
        <li>でも飽きるのも早い</li>
      </ul>
    </section>

    <section class="card" id="diagnosis">
      <h2>ENTP度診断</h2>
      <p>当てはまるものを選んでください。</p>
      <div id="quiz"></div>
      <button id="resultButton">結果を見る</button>
      <div id="result" class="result"></div>
    </section>

    <section class="card" id="debate">
      <h2>ディベートゲーム</h2>
      <p>お題を選んで、どちらかの立場で30秒主張してみよう。</p>
      <div id="debateGame"></div>
    </section>
  </main>

  <footer>
    <p>© ENTP.JP - created for curious chaos.</p>
  </footer>
`;

const quiz = document.querySelector("#quiz");

questions.forEach((q, index) => {
  const item = document.createElement("label");
  item.className = "question";
  item.innerHTML = `
    <input type="checkbox" data-type="${q.type}">
    <span>${index + 1}. ${q.text}</span>
  `;
  quiz.appendChild(item);
});

document.querySelector("#resultButton").addEventListener("click", () => {
  const checked = document.querySelectorAll("#quiz input:checked");
  const score = checked.length;

  let title = "";
  let description = "";

  if (score <= 1) {
    title = "穏やかな観察者";
    description = "ENTP度は低め。混沌を見るのは好きだけど、自分から火をつけるタイプではなさそう。";
  } else if (score <= 3) {
    title = "擬態ENTP";
    description = "ENTPっぽさあり。議論や発想は好きだけど、まだ理性が勝っている。えらい。";
  } else if (score <= 5) {
    title = "かなりENTP";
    description = "退屈を壊す才能あり。面白そうな話題を見ると、黙っていられないタイプ。";
  } else {
    title = "純正カオスENTP";
    description = "危険。アイデアと屁理屈で場を支配するタイプ。議論の火種を持ち歩くな。";
  }

  document.querySelector("#result").innerHTML = `
    <h3>${title}</h3>
    <p>${description}</p>
  `;
});

const debateGame = document.querySelector("#debateGame");

debateTopics.forEach((topic) => {
  const box = document.createElement("div");
  box.className = "debate-topic";
  box.innerHTML = `
    <h3>${topic.title}</h3>
    <button>${topic.a}</button>
    <button>${topic.b}</button>
    <textarea placeholder="ここに主張を書け。遠慮したら負け。"></textarea>
  `;
  debateGame.appendChild(box);
});
